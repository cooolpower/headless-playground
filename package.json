{
  "name": "headless-playground-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter playground build",
    "start": "pnpm --filter playground start",
    "lint": "pnpm --filter playground lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md,mdx}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,css,md,mdx}\"",
    "type-check": "pnpm --filter playground type-check"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "typescript": "^5"
  }
}
