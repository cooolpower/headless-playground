'use client';

// DEPRECATED: Option B 적용으로 recipes는 src/content/recipes 가 canonical 입니다.
// 기존 import 경로 호환을 위해 re-export만 제공합니다.
export {
  CountdownInteractiveControls,
  CountdownInteractivePreview,
  CountdownInteractiveProvider,
} from '@/content/recipes/countdown/interactive-countdown';

