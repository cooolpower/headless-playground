# Textarea

여러 줄 텍스트 입력을 위한 텍스트 영역 컴포넌트입니다.

### 사용법

```tsx
import { Textarea } from '@cooolpower/headless-ui';

export function MyTextarea() {
  return <Textarea placeholder="텍스트를 입력하세요..." />;
}
```

import {
  DemoTextareaBasic,
  DemoTextareaBasicWithControls,
  DemoTextareaBasicControls,
  DemoTextareaBasicProvider,
  DemoTextareaSizes,
  DemoTextareaFeatures,
  DemoTextareaResize,
  DemoTextareaStatus,
  DemoTextareaAutoResize,
} from './textarea.demo';
import { Playground } from '@/components/playground/playground';
import { CssSnippet } from '@/components/playground/css-snippet';
import { TextareaCss } from '@cooolpower/headless-ui';

### 예제

#### 기본 Textarea (인터랙티브)

<DemoTextareaBasicProvider>
  <Playground
    preview={<DemoTextareaBasicWithControls />}
    controls={<DemoTextareaBasicControls />}
  />
</DemoTextareaBasicProvider>

#### 기본 사용법

<Playground preview={<DemoTextareaBasic />} />

```tsx
<Textarea placeholder="기본 텍스트 영역" />

<Textarea
  placeholder="여러 줄 텍스트 입력"
  rows={5}
/>
```

#### 크기 (Size)

<Playground preview={<DemoTextareaSizes />} />

```tsx
<Textarea size="small" placeholder="Small 크기" />
<Textarea size="medium" placeholder="Medium 크기" />
<Textarea size="large" placeholder="Large 크기" />
```

#### 기능 (Features)

<Playground preview={<DemoTextareaFeatures />} />

```tsx
// 지우기 버튼이 있는 텍스트 영역
<Textarea placeholder="지우기 버튼이 있는 텍스트 영역" clearable />

// 최대 길이 제한
<Textarea placeholder="최대 100자까지 입력 가능" maxLength={100} />
```

#### 크기 조절 (Resize)

<Playground preview={<DemoTextareaResize />} />

```tsx
// 크기 조절 불가
<Textarea placeholder="크기 조절 불가" resize="none" />

// 가로/세로 모두 조절 가능
<Textarea placeholder="가로/세로 모두 조절 가능" resize="both" />

// 가로만 조절 가능
<Textarea placeholder="가로만 조절 가능" resize="horizontal" />

// 세로만 조절 가능 (기본값)
<Textarea placeholder="세로만 조절 가능" resize="vertical" />
```

#### 상태 (Status)

<Playground preview={<DemoTextareaStatus />} />

```tsx
// 비활성화
<Textarea value="비활성화된 텍스트 영역" disabled />

// 읽기 전용
<Textarea value="읽기 전용 텍스트 영역" readonly />
```

#### 자동 크기 조절 (Auto Resize)

<Playground preview={<DemoTextareaAutoResize />} />

```tsx
// 텍스트 입력 시 자동으로 높이가 늘어남
<Textarea
  placeholder="텍스트를 입력하면 자동으로 높이가 늘어납니다..."
  autoResize
/>
```

### API

#### Textarea Props

| Property             | Type                                                                      | Default      | Description                            |
| -------------------- | ------------------------------------------------------------------------- | ------------ | -------------------------------------- |
| size                 | `'custom' \| 'small' \| 'medium' \| 'large'`                              | `'medium'`   | 텍스트 영역 크기                       |
| disabled             | `boolean`                                                                 | `false`      | 비활성화 여부                          |
| readonly             | `boolean`                                                                 | `false`      | 읽기 전용 여부                         |
| placeholder          | `string`                                                                  | -            | 플레이스홀더 텍스트                    |
| value                | `string`                                                                  | -            | 제어된 값                              |
| defaultValue         | `string`                                                                  | `''`         | 기본값                                 |
| onChange             | `(value: string, event?: React.ChangeEvent<HTMLTextAreaElement>) => void` | -            | 값 변경 핸들러                         |
| onFocus              | `(event: React.FocusEvent<HTMLTextAreaElement>) => void`                  | -            | 포커스 핸들러                          |
| onBlur               | `(event: React.FocusEvent<HTMLTextAreaElement>) => void`                  | -            | 블러 핸들러                            |
| onKeyDown            | `(event: React.KeyboardEvent<HTMLTextAreaElement>) => void`               | -            | 키 다운 핸들러                         |
| onClick              | `(event: React.MouseEvent<HTMLTextAreaElement>) => void`                  | -            | 클릭 핸들러                            |
| clearable            | `boolean`                                                                 | `false`      | 지우기 버튼 표시 여부                  |
| maxLength            | `number`                                                                  | -            | 최대 길이                              |
| minLength            | `number`                                                                  | -            | 최소 길이                              |
| required             | `boolean`                                                                 | `false`      | 필수 여부                              |
| autoComplete         | `string`                                                                  | -            | 자동완성 속성                          |
| autoFocus            | `boolean`                                                                 | `false`      | 자동 포커스 여부                       |
| name                 | `string`                                                                  | -            | 텍스트 영역 name 속성                  |
| id                   | `string`                                                                  | -            | 텍스트 영역 id 속성                    |
| rows                 | `number`                                                                  | -            | 행 수                                  |
| cols                 | `number`                                                                  | -            | 열 수                                  |
| resize               | `'none' \| 'both' \| 'horizontal' \| 'vertical'`                          | `'vertical'` | 크기 조절 방향                         |
| autoResize           | `boolean`                                                                 | `false`      | 텍스트 입력 시 자동으로 높이 조절 여부 |
| injectStyles         | `boolean`                                                                 | `true`       | 기본 스타일(CSS) 주입 여부             |
| className            | `string`                                                                  | -            | 추가 CSS 클래스                        |
| textareaClassName    | `string`                                                                  | -            | 텍스트 영역 요소 CSS 클래스            |
| textareaStyle        | `React.CSSProperties`                                                     | -            | 텍스트 영역 요소 인라인 스타일         |
| clearButtonClassName | `string`                                                                  | -            | 지우기 버튼 CSS 클래스                 |
| style                | `React.CSSProperties`                                                     | -            | 컨테이너 인라인 스타일                 |

### 특징 (Features)

- **다양한 크기**: small, medium, large 크기 지원
- **상태 관리**: disabled, readonly 상태 지원
- **지우기 기능**: clearable 옵션으로 지우기 버튼 표시
- **길이 제한**: maxLength, minLength로 입력 길이 제한
- **크기 조절**: resize 옵션으로 크기 조절 방향 제어
- **자동 크기 조절**: autoResize 옵션으로 텍스트 입력 시 자동으로 높이 조절
- **접근성**: ARIA 속성 및 키보드 네비게이션 지원

### 접근성 (Accessibility)

- 키보드로 모든 기능 접근 가능
- 스크린 리더 지원
- 적절한 ARIA 속성 사용
- 포커스 관리

### 커스텀 스타일링 (Custom Styling)

`injectStyles={false}`로 설정하면 라이브러리의 기본 스타일이 주입되지 않습니다. 이 경우 사용자가 외부 CSS나 CSS-in-JS를 통해 직접 스타일을 정의해야 합니다.

`Textarea` 컴포넌트는 내부적으로 다음과 같은 고정 클래스명을 제공합니다.

> [!NOTE]
> **제공되는 내부 클래스:**
>
> - `.hcTextareaWrap`: 텍스트 영역과 버튼을 감싸는 루트 컨테이너
> - `.hcTextarea`: 실제 `textarea` 요소
> - `.hcTextareaClear`: 지우기 버튼 요소

#### Vanilla Extract 예제

```typescript
import { style, globalStyle } from '@vanilla-extract/css';

export const customTextareaWrapper = style({});

// 텍스트 영역 스타일 커스텀
globalStyle(`${customTextareaWrapper} .hcTextarea`, {
  backgroundColor: '#f7fafc',
  border: '1px solid #e2e8f0',
  borderRadius: '8px',
  padding: '1rem',
  minHeight: '200px',
  resize: 'none',
});

// 포커스 시 스타일 커스텀
globalStyle(`${customTextareaWrapper} .hcTextarea:focus`, {
  borderColor: '#63b3ed',
  boxShadow: '0 0 0 3px rgba(99, 179, 237, 0.2)',
});
```

### 기본 스타일(CSS) (Inject Styles)

<CssSnippet css={TextareaCss} title="TextareaCss" />

### 모범 사례 (Best Practices)

- 긴 텍스트 입력이 필요한 경우 Textarea 사용
- 사용자에게 입력 길이 제한이 있는 경우 `maxLength` 사용
- 폼 제출 시 필수 입력인 경우 `required` 사용
- 크기 조절이 불필요한 경우 `resize="none"` 사용
