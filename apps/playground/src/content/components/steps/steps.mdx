import {
  DemoStepsBasic,
  DemoStepsBasicWithControls,
  DemoStepsBasicControls,
  DemoStepsBasicProvider,
  DemoStepsWithDescription,
  DemoStepsVertical,
  DemoStepsError,
} from './steps.demo';
import { Playground } from '@/components/playground/playground';
import { CssSnippet } from '@/components/playground/css-snippet';
import { StepsCss } from '@repo/ui';

# Steps

단계별 진행 상황을 표시하는 컴포넌트입니다. 프로세스의 현재 단계와 완료 상태를 시각적으로 표현합니다.

### 사용법

```tsx
import { Steps } from '@repo/ui';

export function MyComponent() {
  return (
    <Steps
      items={[
        { title: '시작', description: '프로세스 시작' },
        { title: '진행 중', description: '처리 중' },
        { title: '완료', description: '프로세스 완료' },
      ]}
      current={1}
    />
  );
}
```

### 예제

#### 기본 Steps (인터랙티브)

<DemoStepsBasicProvider>
  <Playground
    preview={<DemoStepsBasicWithControls />}
    controls={<DemoStepsBasicControls />}
  />
</DemoStepsBasicProvider>

#### 기본 Steps

<Playground preview={<DemoStepsBasic />} />

```tsx
<Steps
  items={[
    { title: '주문 접수' },
    { title: '결제 완료' },
    { title: '배송 중' },
    { title: '배송 완료' },
  ]}
  current={1}
/>
```

#### 설명과 함께

<Playground preview={<DemoStepsWithDescription />} />

```tsx
<Steps
  items={[
    { title: '정보 입력', description: '기본 정보를 입력하세요' },
    { title: '검토', description: '입력 내용을 확인하세요' },
    { title: '완료', description: '처리가 완료되었습니다' },
  ]}
  current={0}
/>
```

#### 세로 방향

<Playground preview={<DemoStepsVertical />} />

```tsx
<Steps
  direction="vertical"
  items={[{ title: '1단계' }, { title: '2단계' }, { title: '3단계' }]}
  current={1}
/>
```

#### 오류 상태

<Playground preview={<DemoStepsError />} />

```tsx
<Steps
  items={[
    { title: '완료', status: 'finish' },
    { title: '오류', status: 'error' },
    { title: '대기', status: 'wait' },
  ]}
  current={1}
/>
```

### API Reference

#### Steps Props

| Property     | Type                                         | Default        | Description           |
| ------------ | -------------------------------------------- | -------------- | --------------------- |
| items        | `StepItem[]`                                 | `[]`           | Step 항목 배열        |
| current      | `number`                                     | `0`            | 현재 step 인덱스      |
| direction    | `'horizontal' \| 'vertical'`                 | `'horizontal'` | Step 방향             |
| size         | `'small' \| 'medium' \| 'large'`             | `'medium'`     | Step 크기             |
| status       | `'wait' \| 'process' \| 'finish' \| 'error'` | -              | 현재 step 상태        |
| onChange     | `(current: number) => void`                  | -              | Step 변경 콜백        |
| className    | `string`                                     | -              | 추가 CSS 클래스       |
| children     | `ReactNode`                                  | -              | 커스텀 콘텐츠         |
| injectStyles | `boolean`                                    | `true`         | 기본 스타일 주입 여부 |

#### StepItem

| Property    | Type                                         | Default | Description        |
| ----------- | -------------------------------------------- | ------- | ------------------ |
| title       | `ReactNode`                                  | -       | Step 제목          |
| description | `ReactNode`                                  | -       | Step 설명          |
| icon        | `ReactNode`                                  | -       | 커스텀 아이콘      |
| status      | `'wait' \| 'process' \| 'finish' \| 'error'` | -       | Step 상태          |
| disabled    | `boolean`                                    | `false` | Step 비활성화 여부 |

## 기본 스타일(CSS) (Inject Styles)

<CssSnippet css={StepsCss} title="StepsCss" />
