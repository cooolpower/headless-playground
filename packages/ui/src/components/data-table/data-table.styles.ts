export const dataTableCss = `
.hcDataTable {
  padding: var(--spacing-base);
  border: 1px solid var(--color-divider);
  border-radius: var(--radius-md);
  background: var(--color-surface);
}

.hcDataTable[data-disabled="true"] {
  opacity: 0.5;
  cursor: not-allowed;
}
`;

