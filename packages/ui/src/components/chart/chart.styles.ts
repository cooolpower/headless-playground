export const chartCss = `
.hcChart {
  padding: var(--spacing-base);
  border: 1px solid var(--color-divider);
  border-radius: var(--radius-md);
  background: var(--color-surface);
  width: 100%;
  height: 100%;
}

.hcChart[data-disabled="true"] {
  opacity: 0.5;
}
`;

